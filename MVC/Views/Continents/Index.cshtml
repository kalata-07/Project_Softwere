@model IEnumerable<BusinessLayer.Continent>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ContinentName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ContinentName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ContinentCode">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ContinentCode">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ContinentCode">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
